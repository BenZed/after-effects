import { assert } from 'chai'

/* globals describe it */

// A lot of these tests will likely be redundant, but in my experience the
// extendscript javascript implementation differs enough to warrant some
// additional scrutiny
describe('ExtendScript ES5 Shim', () => {
  describe('Array shims', () => {
    it('Array.isArray detects Arrays')
    // and so on
  })
})
